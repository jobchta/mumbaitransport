// API Key Configuration for Mumbai Transport App - Example

// 1. Copy this file to `config.js`.
// 2. Replace the placeholder value with your actual Google Maps API key.
// 3. The `config.js` file is ignored by git, so your key will be kept private.

const googleMapsApiKey = 'YOUR_GOOGLE_MAPS_API_KEY_HERE';
